@model IEnumerable<Project.Models.Furnizues>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Liste e Furnizuesve</h1>
@if (User.IsInRole("Admin"))
{
    <p>
        <a asp-action="Create">Krijo Furnizues te ri</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Emri)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shteti)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    <a asp-controller="Produkt" asp-action="FurnizuesProdukte" asp-route-emri="@item.Emri">@item.Emri</a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shteti)
            </td>
            <td>
                    @if (User.IsInRole("Admin")){
                    
                <a asp-action="Edit" asp-route-id="@item.Id">Edito</a> 
                <a asp-action="Delete" asp-route-id="@item.Id">Fshi</a>
                    }                    
            </td>
        </tr>
}
    </tbody>
</table>
